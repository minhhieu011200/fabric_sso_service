package com.example.sso_service.service;

public class PinService {

    // Verify OTP
    public boolean verifyPin(String userIdentifier, String pin) {
//        String storedOtp = otpStorage.get(userIdentifier);
        return true;
    }
}
